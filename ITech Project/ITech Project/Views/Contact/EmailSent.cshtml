@model MailRequestDto

<div class="container mt-3 d-flex flex-column justify-content-center align-items-center" style="height: 500px">
    <h4 class="d-inline">Email has been sent by: </h4>
    <p class="text-success text-wrap">@Model.ToEmail</p>
    <p class="mt-3">we have received your issue and we will reply to you as soon as possible</p>
    <a asp-controller="home" asp-action="index" class="btn btn-secondary">Return to home</a>
</div>